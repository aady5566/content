[slide]

**機器學習、深度學習基本原理**

2025-09

[slide]

解決問題的思維轉變：指令 ➞ 推論

傳統程式設計（就是計算機啦）：輸入 ➞ 人類規則 ➞ 輸出

機器學習 (Machine Learning)：提供大量資料 ➞ 找到規則

[slide]

機器學習的主要類型

監督式學習 (Supervised Learning)

- **資料類型：** 使用帶有「標籤」的資料進行訓練，即每個輸入資料都有對應的正確輸出或答案。
- **目標：** 學習輸入資料與輸出資料之間的映射關係，以便在遇到新數據時能預測其輸出結果。
- **應用範例：** 根據過去的天氣狀況（輸入）預測未來的航班時間（輸出）。

非監督式學習 (Unsupervised Learning) 

- **資料類型：**: 使用未帶有標籤的原始資料進行訓練。
- **目標：**: 探索資料並在其中發現隱藏的模式、結構或關係，而不是預測特定的輸出。
- **應用範例：**: 尋找顧客群體中的相似性（分群），或識別未標記資料中的新關聯。

[slide]

（參考 w3-h1.html <section id="ml" class="py-16 md:py-24"> 的內容，並將配色根據 [style.md](http://style.md) 進行調整並重現它）

[slide]

（插入：gen_stuff/w3-supervised-learning.html 並將配色根據 [style.md](http://style.md) 進行調整）

[slide]

（插入：gen_stuff/w3-unsupervised-learning.html 並將配色根據 [style.md](http://style.md) 進行調整）

[slide]

那深度學習 (Deep Learning) 又是什麼？

[slide]

深度學習(Deep Learning)

- **與機器學習的關係：** 是機器學習的一個子集，屬於一類特定的機器學習模型。
- **特點：** 使用多層神經網路（類似人類大腦結構）來學習資料中的複雜模式。
- **應用範例：** 深度學習模型可以執行監督式學習任務，如圖像識別，也可以進行非監督式學習，從大量的未標記資料中提取有價值的資訊。

[slide]

那…生成式 AI (Generative AI) 又是什麼？

[slide]

生成式 AI 是基於深度學習的技術分支，可以學習數據中的模式並生成新的內容，涵蓋文本、圖像、聲音等多種形式。（細節下堂課再介紹。）

[slide]

（參考 w3-h1.html 之 <section id="summary" class="py-16 md:py-24"> 內容，並將配色根據 [style.md](http://style.md) 進行調整並重現它並重現它)

[slide]

機器學習的核心：

**監督式學習 (Supervised Learning)** 與 **非監督式學習 (Unsupervised Learning)**

技術路徑的選擇，更反映了我們想從數據中獲得什麼是「驗證已知的答案」，還是「探索未知的結構」

[slide]

關鍵問題

「有標準答案的考卷」（監督式學習）來訓練 AI 解決已知的問題，比起「沒有答案的浩瀚圖書館」（非監督式學習）中探索未知和關聯性，如何在「指導」與「探索」之間的權衡？又有哪些風險？

[slide]

（可以分成四個 block，使用 bunch-word-container）

不同情境下的探討

1. 個人化的音樂世界： Spotify 如何為你推薦音樂？一種方式是監督式：你按了「喜歡」的歌曲（提供了明確標籤），系統就去尋找音樂特徵相似的歌曲推薦給你。另一種是非監督式：系統分析全球數億用戶的收聽數據，將品味相似的人自動「分群 (Clustering)」，然後把你那一群人喜歡、但你還沒聽過的歌推薦給你（例如「每週新發現」）。哪一種方式更容易讓你跳出同溫層，發現全新的音樂類型？哪一種方式又可能讓你越陷越深？
2. 酷澎電商的行銷策略： 酷澎網站想了解顧客。監督式做法是，進行滿意度問卷，讓顧客在「價格」、「服務」、「品質」等項目上打 1-5 分（給予明確標籤），然後分析哪個環節分數最低。非監督式做法是，直接分析數十萬條顧客的原始評論文字，AI 自動將評論分群，可能會發現一個從未想過的顧客抱怨主題，例如「包裝過度不環保」或「贈品品質太差」，這在原本的問卷中根本沒有這個選項。哪種方法能解決已知的問題？哪種能發現未知的問題？
3. 打擊金融詐騙： 銀行如何偵測盜刷？監督式方法是，利用過去所有被標記為「盜刷」和「正常」的交易紀錄來訓練模型，當一筆新交易進來時，模型會判斷其為盜刷的機率。但如果出現一種全新的詐騙手法，模型可能就認不出來。非監督式的「異常偵測 (Anomaly Detection)」則是，模型先學習什麼是「正常」的交易模式（一個龐大的群體），任何不符合這個模式的交易，無論手法為何，都會被標記為可疑。哪種方法更擅長防堵已知的敵人？哪種更能應對未知的威脅？
4. 城市安全的洞察： 警察局想預防犯罪。監督式方法是，分析過去標記有「犯罪地點」和「時間」的數據，來預測下一個犯罪熱點。但這完全依賴於「有報案」的數據。非監督式方法則是，分析城市裡匿名的通訊或交通流量數據，找出「異常聚集」或「模式突然改變」的區域，這可能是一個未被發現的地下交易市場，也可能只是一場快閃活動。我們該如何權衡這種探索帶來的機會與誤判的風險？

[slide]

看不見的風險：從「偏見的固化」到「虛假的發現」

[slide]

- **監督式風險 -** 偏見問題（The Bias Problem)
AI 從有偏見的歷史數據中學習，不僅會複製，更會放大這些偏見。原則：垃圾進，垃圾出。

[slide]

（繪製 bar chart，縱軸 0-100%，橫軸淺膚色男性，深膚色女性）

- 淺膚色男性：辨識錯誤率 0.5%
- 深膚色女性：錯誤率 34.7%

（並插入圖片 /pic/w3-p1.jpg ，放置於 bar chart 右邊）

![image.png](attachment:a30e0f8a-5974-4e08-b542-ee4726f0668c:image.png)

ref: （MIT）Media Lab 的《Gender Shades》研究（Joy Buolamwini）

[slide]

- **非監督式風險 - 詮釋的困難與虛假關聯 (Difficulty in Interpretation & Spurious Correlations)：** 非監督式學習找到的「模式」不一定具有現實意義。它可能會發現冰淇淋銷量和溺水人數呈現高度正相關。（相關不等於因果）。

[slide]

還有一種「強化學習」(Reinforcement Learning, RL)，

- 不需要標籤比對，也不像非監督學習單純從數據自發發現模式。
- 透過環境互動，根據行為得到的獎勵或懲罰來漸進地學習達成目標的策略。（就跟人類很像）

[slide]

用強化學習來通關玩寶可夢

（插入 youtube 影片：https://www.youtube.com/watch?v=DcYLT37ImBY&t=80s）

start 0:00-4:20 規則

start 5:25-6:24 AI 模型的優化

start 6:39-7:30 大量探索

[slide]

考試？

[slide]

課堂作業：生活中的 AI 偵探

google form 連結：（之後會給）

**請於 9/25 周四 23:59 前完成提交**

**簡短說明：** 請找出一個生活中的 AI 應用，並回答以下問題。

1. **你的姓名與學號：**
    - [簡答]
2. **你發現了哪個 AI 應用？** (例如：YouTube 的影片推薦系統、Instagram 的限時動態濾鏡、Google Maps 的預估到達時間、蝦皮的「猜你喜歡」商品)
    - [簡答]
3. **簡要描述這個 AI 應用是如何運作的？它在「學習」什麼？** (例如：它學習我看過哪些影片、停留多久，來推薦我可能喜歡的新影片。)
    - [段落]
4. **這個 AI 應用為你帶來了什麼「好處」？** (例如：節省我尋找影片的時間、讓我發現新的興趣、讓我的照片更好看。)
    - [段落]
5. **根據課堂上學到的概念（如：偏見、黑盒子、信任），思考這個 AI 應用可能存在的一個「潛在風險」或「負面影響」？** (例如：它可能讓我陷入「同溫層」，只看見我想看的內容，視野越來越窄。或是，美顏濾鏡可能加劇了容貌焦慮。)
    - [段落]

參考文件：

- w3-h1.html

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI 核心概念導覽：從機器學習到生成式 AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Chosen Palette: Calm Harmony -->
    <!-- Application Structure Plan: A single-page, vertical scrolling journey designed for educational flow. It starts with a hero section, then sequentially introduces Machine Learning (now with supervised/unsupervised tabs), Deep Learning, and Generative AI (with a real Gemini API call) in dedicated, interactive sections. A final summary visualizes their relationship. This narrative structure is chosen over a dashboard because the goal is to guide a beginner through related concepts step-by-step, making complex information digestible and engaging. User flow is primarily linear scrolling, with top navigation for quick access. -->
    <!-- Visualization & Content Choices:
        1. Report Info: ML is like teaching a child to recognize cats/dogs. Goal: Inform & Engage. Viz/Method: Interactive HTML/CSS "image" classification game with buttons. Interaction: User clicks a button to classify an icon, receiving instant feedback. Justification: This makes the abstract concept of "pattern recognition" tangible and memorable. Library/Method: Vanilla JS, HTML, Tailwind.
        2. Report Info: Supervised vs. Unsupervised Learning. Goal: Compare & Inform. Viz/Method: A clean tabbed interface. Interaction: User clicks tabs to switch between detailed explanations of each learning type. Justification: Allows for direct comparison of concepts within the same context, reinforcing understanding. Library/Method: Vanilla JS, HTML, Tailwind.
        3. Report Info: DL as a complex ML model. Goal: Organize & Inform. Viz/Method: A simplified neural network diagram built with HTML/CSS divs. Interaction: Hovering over nodes reveals tooltips explaining their function. Justification: Visualizes the layered structure of DL. Library/Method: Vanilla JS, HTML, Tailwind.
        4. Report Info: GenAI as a creative "intern". Goal: Demonstrate & Engage. Viz/Method: An interactive chat interface that calls the Gemini API. Interaction: User selects a prompt and clicks a button, which triggers a live API call to generate and display new content. Justification: Provides a real, powerful demonstration of GenAI's capabilities, moving beyond simulation. Library/Method: Vanilla JS (Fetch API).
        5. Report Info: Relationship between ML, DL, and GenAI. Goal: Summarize & Organize. Viz/Method: An updated nested block diagram. Interaction: Hovering over each block provides a summary definition. Justification: Clearly illustrates the hierarchical and subset relationships for a final, consolidated understanding. Library/Method: Vanilla JS, HTML, Tailwind.
    -->
    <!-- CONFIRMATION: NO SVG graphics used. NO Mermaid JS used. -->
    <style>
        body {
            font-family: 'Noto Sans TC', sans-serif;
        }
        .smooth-scroll {
            scroll-behavior: smooth;
        }
        .tab-btn {
            transition: all 0.3s ease-in-out;
        }
        .tab-btn.active {
            border-color: #0ea5e9; /* sky-500 */
            color: #0c4a6e; /* sky-900 */
            background-color: #e0f2fe; /* sky-100 */
        }
        .tab-content {
            display: none;
        }
        .tab-content.active {
            display: block;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .fade-in {
            animation: fadeIn 0.5s ease-in-out forwards;
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-800 smooth-scroll">

    <header class="bg-white/80 backdrop-blur-md sticky top-0 z-50 shadow-sm">
        <nav class="container mx-auto px-6 py-3 flex justify-between items-center">
            <div class="text-xl font-bold text-sky-800">
                AI 探索之旅
            </div>
            <div class="hidden md:flex space-x-6">
                <a href="#ml" class="text-slate-600 hover:text-sky-600 transition-colors duration-300">機器學習</a>
                <a href="#dl" class="text-slate-600 hover:text-sky-600 transition-colors duration-300">深度學習</a>
                <a href="#genai" class="text-slate-600 hover:text-sky-600 transition-colors duration-300">生成式 AI</a>
                <a href="#summary" class="text-slate-600 hover:text-sky-600 transition-colors duration-300">總覽</a>
            </div>
            <div class="md:hidden">
                <select id="mobile-nav" class="bg-slate-100 border border-slate-300 rounded-md p-2 text-slate-700">
                    <option value="#home">首頁</option>
                    <option value="#ml">機器學習</option>
                    <option value="#dl">深度學習</option>
                    <option value="#genai">生成式 AI</option>
                    <option value="#summary">總覽</option>
                </select>
            </div>
        </nav>
    </header>

    <main class="container mx-auto px-6 py-12 md:py-20">

        <section id="home" class="text-center min-h-[60vh] flex flex-col justify-center items-center">
            <h1 class="text-4xl md:text-6xl font-bold text-sky-900 leading-tight">解密人工智慧</h1>
            <p class="mt-4 text-lg md:text-xl text-slate-600 max-w-3xl">從學習規律到創造內容，讓我們用最生活化的方式，帶您一探機器學習、深度學習與生成式 AI 的奧秘。</p>
            <a href="#ml" class="mt-8 px-8 py-3 bg-sky-600 text-white font-semibold rounded-full shadow-lg hover:bg-sky-700 transition-transform duration-300 transform hover:scale-105">
                啟程探索
            </a>
        </section>

        <section id="ml" class="py-16 md:py-24">
            <div class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-bold text-sky-800">核心概念一：機器學習 (ML)</h2>
                <p class="mt-3 text-lg text-slate-600 max-w-2xl mx-auto">讓機器學會舉一反三</p>
            </div>
            <div class="prose max-w-4xl mx-auto text-slate-700 mb-12">
                <p>機器學習的核心精神，是讓電腦系統能夠在沒有明確程式指令下，透過學習資料來執行任務。它旨在從大量數據中識別模式、趨勢，並做出準確的預測或分類。這就像我們教一個小孩辨識貓狗，透過不斷給他看範例，讓他自己「歸納」出特徵與「規律」。</p>
            </div>

            <div class="grid md:grid-cols-2 gap-12 items-start">
                <div class="bg-white p-8 rounded-2xl shadow-lg border border-slate-200">
                    <h3 class="text-xl font-semibold text-center mb-4">主要學習類型</h3>
                    <div class="border-b border-slate-200 mb-4">
                        <nav class="-mb-px flex space-x-4" aria-label="Tabs">
                            <button id="tab-supervised" class="tab-btn active whitespace-nowrap py-3 px-1 border-b-4 font-medium text-lg">
                                監督式學習
                            </button>
                            <button id="tab-unsupervised" class="tab-btn whitespace-nowrap py-3 px-1 border-b-4 border-transparent text-slate-500 hover:text-slate-700 hover:border-slate-300 font-medium text-lg">
                                非監督式學習
                            </button>
                        </nav>
                    </div>
                    <div>
                        <div id="content-supervised" class="tab-content active fade-in">
                            <h4 class="font-bold text-sky-800 text-lg mb-2">使用「有標籤」的資料</h4>
                            <p class="text-slate-600 mb-4">每個輸入資料都有對應的「正確答案」，像帶著標準答案的練習冊。</p>
                            <strong class="text-slate-800">目標：</strong>
                            <p class="text-slate-600 mb-3">學習輸入與輸出之間的對應關係，以便預測新資料的結果。</p>
                            <strong class="text-slate-800">範例：</strong>
                            <p class="text-slate-600">根據過去的天氣狀況（輸入）和對應的航班延誤記錄（標籤），來預測未來航班是否會準時（輸出）。</p>
                        </div>
                        <div id="content-unsupervised" class="tab-content fade-in">
                           <h4 class="font-bold text-amber-800 text-lg mb-2">使用「沒有標籤」的資料</h4>
                           <p class="text-slate-600 mb-4">只有一堆原始資料，沒有現成的答案，像一本沒有註解的古文書。</p>
                           <strong class="text-slate-800">目標：</strong>
                           <p class="text-slate-600 mb-3">在資料中探索並發現隱藏的模式、結構或群體。</p>
                           <strong class="text-slate-800">範例：</strong>
                           <p class="text-slate-600">分析顧客的購買紀錄，自動將行為相似的顧客分為不同群體（分群），以進行精準行銷。</p>
                        </div>
                    </div>
                </div>
                 <div class="bg-white p-8 rounded-2xl shadow-lg border border-slate-200">
                    <h3 class="text-xl font-semibold text-center mb-4">互動體驗：教 AI 辨識動物</h3>
                    <p class="text-sm text-center text-slate-500 mb-4">這就是一個「監督式學習」的簡單例子！</p>
                    <div id="ml-game-display" class="text-8xl text-center p-8 bg-slate-100 rounded-xl my-4 flex justify-center items-center h-48">
                        <span>🐱</span>
                    </div>
                    <p id="ml-game-feedback" class="text-center h-6 mb-4 font-medium">&nbsp;</p>
                    <div class="flex justify-center gap-4">
                        <button id="ml-cat-btn" class="w-full py-3 px-6 bg-amber-500 text-white font-bold rounded-lg shadow hover:bg-amber-600 transition-all duration-300 transform hover:scale-105">這是 貓 🐱</button>
                        <button id="ml-dog-btn" class="w-full py-3 px-6 bg-cyan-600 text-white font-bold rounded-lg shadow hover:bg-cyan-700 transition-all duration-300 transform hover:scale-105">這是 狗 🐶</button>
                    </div>
                </div>
            </div>
        </section>

        <section id="dl" class="py-16 md:py-24 bg-slate-100 -mx-6 px-6 rounded-3xl">
            <div class="container mx-auto">
                <div class="text-center mb-12">
                    <h2 class="text-3xl md:text-4xl font-bold text-sky-800">更進一步：深度學習 (DL)</h2>
                    <p class="mt-3 text-lg text-slate-600 max-w-2xl mx-auto">模仿大腦，學得更深</p>
                </div>
                <div class="grid md:grid-cols-2 gap-12 items-center">
                    <div class="bg-white p-8 rounded-2xl shadow-lg border border-slate-200 flex flex-col justify-center items-center h-full">
                        <h3 class="text-xl font-semibold text-center mb-6">簡化神經網路示意圖</h3>
                        <div class="relative w-full h-64 flex justify-between items-center px-4">
                            <div class="flex flex-col justify-around h-full">
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-sky-200 text-sky-800" data-tooltip="輸入層：接收原始資料，如圖片的像素。">I-1</div>
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-sky-200 text-sky-800" data-tooltip="輸入層：接收原始資料，如圖片的像素。">I-2</div>
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-sky-200 text-sky-800" data-tooltip="輸入層：接收原始資料，如圖片的像素。">I-3</div>
                            </div>
                            <div class="flex flex-col justify-around h-full">
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-slate-200 text-slate-800" data-tooltip="隱藏層：提取特徵，如邊緣、形狀、紋理。">H1</div>
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-slate-200 text-slate-800" data-tooltip="隱藏層：提取特徵，如邊緣、形狀、紋理。">H2</div>
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-slate-200 text-slate-800" data-tooltip="隱藏層：提取特徵，如邊緣、形狀、紋理。">H3</div>
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-slate-200 text-slate-800" data-tooltip="隱藏層：提取特徵，如邊緣、形狀、紋理。">H4</div>
                            </div>
                            <div class="flex flex-col justify-around h-full">
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-amber-200 text-amber-800" data-tooltip="輸出層：做出最終判斷，如「貓」或「狗」。">O-1</div>
                               <div class="relative w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm cursor-pointer bg-amber-200 text-amber-800" data-tooltip="輸出層：做出最終判斷，如「貓」或「狗」。">O-2</div>
                            </div>
                        </div>
                         <div id="tooltip" class="absolute bg-slate-800 text-white text-xs rounded py-1 px-2 pointer-events-none opacity-0 transition-opacity"></div>
                    </div>
                    <div class="prose max-w-none text-slate-700">
                        <p>深度學習是<strong>機器學習的一個子集</strong>，它的靈感來自人類大腦的神經網路結構。</p>
                        <p>它使用由許多「層」組成的「人工神經網路」來學習。每一層都負責學習一種不同層次的特徵（如點、線、面到具體物件），這種分層、抽象的學習方式，讓 AI 能夠處理非常複雜的資訊。</p>
                        <p>深度學習是一種強大的技術，它既可以執行<strong>監督式學習</strong>任務（如圖像識別），也能進行<strong>非監督式學習</strong>，從大量的未標記資料中提取有價值的資訊。</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="genai" class="py-16 md:py-24">
            <div class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-bold text-sky-800">核心概念二：生成式 AI (GenAI)</h2>
                <p class="mt-3 text-lg text-slate-600 max-w-2xl mx-auto">AI 的創造力</p>
            </div>
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div class="prose max-w-none text-slate-700">
                    <p>如果說傳統的機器學習是個很會「歸納整理」的學生，那麼生成式 AI (GenAI) 就是一位學習了全世界知識、充滿創造力的「超級實習生」。</p>
                    <p>它通常基於複雜的深度學習模型，不僅僅是辨識或分類資料，而是基於它學到的龐大知識庫，根據您的指令去「創造」出全新的、前所未見的內容。從撰寫文案、創作詩歌、產生程式碼，到繪製圖片，生成式 AI 正在徹底改變我們創造內容與互動的方式。</p>
                </div>
                <div class="bg-white p-8 rounded-2xl shadow-lg border border-slate-200">
                    <h3 class="text-xl font-semibold text-center mb-4">互動體驗：呼叫 Gemini 實習生</h3>
                    <div class="bg-slate-100 p-4 rounded-lg min-h-[220px] flex flex-col">
                        <div id="genai-chatbox" class="flex-grow space-y-2 overflow-y-auto" style="max-height: 250px;">
                             <div class="p-3 bg-sky-100 text-sky-900 rounded-lg self-start max-w-xs mr-auto">
                                <span>你好！請從下方選擇一個任務，我將為您生成內容。</span>
                            </div>
                        </div>
                    </div>
                    <div class="mt-4">
                        <label for="genai-prompt-select" class="block text-sm font-medium text-slate-700 mb-2">選擇一個指令：</label>
                        <select id="genai-prompt-select" class="w-full p-2 border border-slate-300 rounded-md">
                            <option value="poem">寫一首關於星空的短詩</option>
                            <option value="slogan">為一家新開的咖啡店想個標語</option>
                            <option value="plan">規劃一個週末的台北一日遊</option>
                        </select>
                        <button id="genai-btn" class="mt-4 w-full py-3 px-6 bg-sky-600 text-white font-bold rounded-lg shadow hover:bg-sky-700 transition-all duration-300 transform hover:scale-105 disabled:bg-slate-400 disabled:cursor-not-allowed">
                            <span class="btn-text">生成內容</span>
                            <span class="btn-spinner hidden animate-spin">●</span>
                        </button>
                    </div>
                </div>
            </div>
        </section>

        <section id="summary" class="py-16 md:py-24">
            <div class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-bold text-sky-800">觀念總結：它們的關係</h2>
                <p class="mt-3 text-lg text-slate-600 max-w-2xl mx-auto">一個層層遞進、不斷演化的家族</p>
            </div>
            <div class="w-full max-w-5xl mx-auto p-4">
                <div class="summary-box bg-sky-100 border-2 border-sky-300 rounded-3xl p-6 md:p-8 transition-all duration-300">
                    <h3 class="text-xl md:text-2xl font-bold text-sky-900">機器學習 (Machine Learning)</h3>
                    <p class="mt-2 text-sky-800">AI 的基礎領域，讓電腦從資料中學習。主要分為「監督式」與「非監督式」兩大類型。</p>

                    <div class="mt-6 summary-box bg-sky-200/80 border-2 border-sky-400 rounded-2xl p-4 md:p-6 transition-all duration-300">
                        <h4 class="text-lg md:text-xl font-bold text-sky-900">深度學習 (Deep Learning)</h4>
                        <p class="mt-1 text-sky-800">是機器學習的子集，使用複雜的多層神經網路。此技術可應用於監督式和非監督式學習任務，處理更複雜的模式。</p>

                        <div class="mt-6 summary-box bg-amber-100/80 border-2 border-amber-400 rounded-2xl p-4 md:p-6 transition-all duration-300">
                            <h5 class="text-lg md:text-xl font-bold text-amber-900">生成式 AI (Generative AI)</h5>
                            <p class="mt-1 text-amber-800">通常基於大型深度學習模型，專注於「創造」全新的內容，而不僅是分析或預測。</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="bg-slate-800 text-slate-300 text-center py-6">
        <p>&copy; 2025 AI 概念探索. All Rights Reserved.</p>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {

            const mobileNav = document.getElementById('mobile-nav');
            mobileNav.addEventListener('change', (e) => {
                const targetId = e.target.value;
                document.querySelector(targetId).scrollIntoView({ behavior: 'smooth' });
            });

            const navLinks = document.querySelectorAll('header a[href^="#"]');
            navLinks.forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    document.querySelector(this.getAttribute('href')).scrollIntoView({ behavior: 'smooth' });
                });
            });

            const mlTabs = {
                buttons: [document.getElementById('tab-supervised'), document.getElementById('tab-unsupervised')],
                contents: [document.getElementById('content-supervised'), document.getElementById('content-unsupervised')],
                init() {
                    this.buttons.forEach((btn, index) => {
                        btn.addEventListener('click', () => this.activateTab(index));
                    });
                },
                activateTab(activeIndex) {
                    this.buttons.forEach((btn, index) => {
                        btn.classList.toggle('active', index === activeIndex);
                    });
                    this.contents.forEach((content, index) => {
                        content.classList.toggle('active', index === activeIndex);
                    });
                }
            };
            mlTabs.init();

            const mlGame = {
                display: document.getElementById('ml-game-display'),
                feedback: document.getElementById('ml-game-feedback'),
                catBtn: document.getElementById('ml-cat-btn'),
                dogBtn: document.getElementById('ml-dog-btn'),
                items: [
                    { icon: '🐱', type: 'cat' }, { icon: '🐶', type: 'dog' },
                    { icon: '🐈', type: 'cat' }, { icon: '🐕', type: 'dog' },
                    { icon: '🐩', type: 'dog' }, { icon: '😺', type: 'cat' },
                ],
                currentIndex: 0,
                init() {
                    this.render();
                    this.catBtn.addEventListener('click', () => this.checkAnswer('cat'));
                    this.dogBtn.addEventListener('click', () => this.checkAnswer('dog'));
                },
                render() {
                    this.display.innerHTML = `<span>${this.items[this.currentIndex].icon}</span>`;
                    this.feedback.textContent = '\u00A0';
                },
                checkAnswer(answer) {
                    if (this.items[this.currentIndex].type === answer) {
                        this.feedback.textContent = '正確！AI 學到了一點！';
                        this.feedback.className = 'text-center h-6 mb-4 font-medium text-green-600';
                    } else {
                        this.feedback.textContent = '不對喔，再試一次！';
                        this.feedback.className = 'text-center h-6 mb-4 font-medium text-red-600';
                    }
                    this.currentIndex = (this.currentIndex + 1) % this.items.length;
                    setTimeout(() => this.render(), 1200);
                }
            };
            mlGame.init();

            const dlDiagramNodes = document.querySelectorAll('#dl [data-tooltip]');
            const tooltip = document.getElementById('tooltip');
            dlDiagramNodes.forEach(node => {
                node.addEventListener('mouseenter', (e) => {
                    tooltip.textContent = e.target.getAttribute('data-tooltip');
                    tooltip.style.opacity = 1;
                    const nodeRect = e.target.getBoundingClientRect();
                    const tooltipRect = tooltip.getBoundingClientRect();
                    tooltip.style.left = `${nodeRect.left + (nodeRect.width / 2) - (tooltipRect.width / 2)}px`;
                    tooltip.style.top = `${nodeRect.top - tooltipRect.height - 5}px`;
                });
                node.addEventListener('mouseleave', () => {
                    tooltip.style.opacity = 0;
                });
            });

            const genAI = {
                select: document.getElementById('genai-prompt-select'),
                btn: document.getElementById('genai-btn'),
                chatbox: document.getElementById('genai-chatbox'),
                btnText: document.querySelector('#genai-btn .btn-text'),
                btnSpinner: document.querySelector('#genai-btn .btn-spinner'),
                prompts: {
                    poem: "請用繁體中文寫一首關於夜晚星空的四行短詩。",
                    slogan: "請用繁體中文，為一家新開的、強調手沖與溫暖氛圍的咖啡店，想一個有創意的標語。",
                    plan: "請用繁體中文，規劃一個適合年輕人的週末台北一日遊行程，包含至少三個景點與餐飲建議。"
                },
                init() {
                    this.btn.addEventListener('click', () => this.generate());
                },
                addMessage(content, sender = 'bot') {
                    const messageWrapper = document.createElement('div');
                    messageWrapper.className = `p-3 rounded-lg max-w-xs fade-in ${
                        sender === 'bot' ? 'bg-slate-200 text-slate-800 ml-auto' : 'bg-sky-100 text-sky-900 mr-auto'
                    }`;
                    messageWrapper.innerHTML = `<span>${content.replace(/\n/g, '<br>')}</span>`;
                    this.chatbox.appendChild(messageWrapper);
                    this.chatbox.scrollTop = this.chatbox.scrollHeight;
                },
                setLoading(isLoading) {
                    this.btn.disabled = isLoading;
                    this.btnText.classList.toggle('hidden', isLoading);
                    this.btnSpinner.classList.toggle('hidden', !isLoading);
                },
                async generate() {
                    const selectedKey = this.select.value;
                    const userPrompt = this.prompts[selectedKey];
                    if (!userPrompt) return;

                    this.setLoading(true);
                    this.addMessage("好的，請稍候...", "bot");

                    const apiKey = "";
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-05-20:generateContent?key=${apiKey}`;

                    const payload = {
                        contents: [{ parts: [{ text: userPrompt }] }]
                    };

                    try {
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });

                        if (!response.ok) {
                           throw new Error(`HTTP 錯誤! 狀態: ${response.status}`);
                        }

                        const result = await response.json();
                        const candidate = result.candidates?.[0];
                        const text = candidate?.content?.parts?.[0]?.text;

                        if (text) {
                            this.addMessage(text, 'bot');
                        } else {
                           this.addMessage('抱歉，無法生成內容。請稍後再試。', 'bot');
                        }

                    } catch (error) {
                        console.error("Gemini API 呼叫失敗:", error);
                        this.addMessage('發生錯誤，請檢查主控台訊息。', 'bot');
                    } finally {
                        this.setLoading(false);
                    }
                }
            };
            genAI.init();

            const summaryBoxes = document.querySelectorAll('.summary-box');
            summaryBoxes.forEach(box => {
                box.addEventListener('mouseenter', () => box.classList.add('shadow-xl', 'scale-105'));
                box.addEventListener('mouseleave', () => box.classList.remove('shadow-xl', 'scale-105'));
            });
        });
    </script>
</body>
</html>

```